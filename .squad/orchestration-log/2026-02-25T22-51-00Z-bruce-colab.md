# Orchestration: Bruce — Colab Training Notebook

**Agent:** Bruce (Vision/ML Dev 🔧)  
**Date:** 2026-02-25T22:51:00Z  
**Session:** squad-onnx-spike-and-camera-pipeline  
**Output:** design/colab-training-notebook.ipynb (committed to main)

## Summary

Complete, runnable Google Colab notebook for training YOLOv8n on resistor dataset and exporting to ONNX. Committed directly to main — no PR required. Covers setup, dataset download, training (50 epochs + early stopping), evaluation, ONNX export, and C# integration notes.

## Notebook Contents

**Section 1 — Setup**
- `ultralytics` + `roboflow` Python packages

**Section 2 — Dataset Download**
- `isha-74mjj/yolov5-u3oks` via Roboflow API (requires free account + API key)

**Section 3 — Training**
- YOLOv8n, 50 epochs, batch=16, T4 GPU (~25–40 min), early stopping patience=10
- Target mAP50 > 0.70 (rerun at 100 epochs if below 0.65)

**Section 4 — Evaluation**
- `model.val()` + metrics printout

**Section 5 — ONNX Export**
- opset 17, `imgsz=640`, `simplify=True`, `dynamic=False` (fixed batch=1 for mobile)
- Output: `resistor-localization.onnx`

**Section 6 — Validation**
- Netron.app inspection instructions
- Asset placement: `src/VivaLaResistance/Resources/Raw/` with build action `MauiAsset`

**Section 7 — C# Integration Notes**
- Input preprocessing (BGRA8888 → RGB float32 CHW)
- Output shape: `[1, 5, 8400]` (cx, cy, w, h, conf)
- Post-inference NMS (conf 0.25, IoU 0.45)

## BLOCKING GATE

**Bruce must verify Roboflow dataset license (CC-BY 4.0 acceptable) BEFORE running notebook.**

Notebook will not execute without Roboflow API key + account access. License verification is the hard gate before training starts.

## Next Actions

1. Bruce: Verify isha-74mjj dataset CC-BY 4.0 license on Roboflow (blocking)
2. Bruce: Once license confirmed, run notebook on Colab → export ONNX
3. Bruce: Place exported model in src/VivaLaResistance/Resources/Raw/ with correct build action
